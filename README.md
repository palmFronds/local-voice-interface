# Simple Voice Interface

A minimal real-time, interruptible voice interface for an autonomous agent (OpenClaw). This project connects an agent loop to streaming speech-to-text and text-to-speech, enabling full duplex conversation with barge-in interruption (the agent stops speaking when the user begins talking).
