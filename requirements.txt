# Simple Voice Interface — Python dependencies
# Install with: pip install -r requirements.txt
# Built against Python 3.11+
#
# Packages are added here incrementally as each build step is completed.
# Step 1 — config.py only

python-dotenv==1.0.1
